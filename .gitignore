# Secrets and local runtime state — never commit.
.env
/data/

# Build output (anchored to repo root — иначе паттерн глотает cmd/yandex2mqtt/)
/bin/
/yandex2mqtt
/yandex2mqtt.exe
*.db
*.db-shm
*.db-wal

# Node reference (legacy) local artifacts, if any reappear
node_modules/
